---
interface Props {
  class?: string;
}
const { class: className = '' } = Astro.props;
---
<span class={`inline-flex items-center px-4 py-1.5 rounded-full bg-primary-light text-primary text-xs font-bold tracking-widest uppercase ${className}`}>
  <slot />
</span>
